org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# --- Release signing (fill these in locally, or via CI secrets — never commit
#     real values; see RELEASE_SIGNING.md). Uncomment and set once you have a
#     keystore, then app/build.gradle.kts will pick them up automatically. ---
# STEPTRACK_RELEASE_STORE_FILE=../keystore/steptrack-release.jks
# STEPTRACK_RELEASE_STORE_PASSWORD=changeme
# STEPTRACK_RELEASE_KEY_ALIAS=steptrack
# STEPTRACK_RELEASE_KEY_PASSWORD=changeme
